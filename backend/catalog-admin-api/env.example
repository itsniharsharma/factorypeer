# Prefer the monorepo root `.env.example` — variables are loaded from
# `../../.env` and `../../.env.local` via `src/bootstrap-env.ts` when the API starts.
# You can still use a local `.env` in this folder for overrides if needed.
